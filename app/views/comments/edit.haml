%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:comments, :index)
  %li.active=link_to tag_icon(:edit, pat(:edit)), url(:comments, :edit, :id => @comment.id)

.tabs-content
  -form_for :comment, url(:comments, :update, :id => @comment.id), :method => :put,  :enctype=>"multipart/form-data",:class => 'form-horizontal' do |f|
    =partial 'comments/form', :locals => { :f => f }
